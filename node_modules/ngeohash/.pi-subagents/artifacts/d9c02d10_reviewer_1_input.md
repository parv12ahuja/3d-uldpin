# Task for reviewer

[Read from: /home/nsun/workspace/node-geohash/plan.md, /home/nsun/workspace/node-geohash/progress.md]

You are doing the SPEC axis of a two-axis code review. Do NOT comment on coding-style/standards — the other axis covers that.

## Repo
Path: /home/nsun/workspace/node-geohash (currently on branch `fix/odd-bitdepth-int-decode`). Fixed point is `master`.

## Diff to review
`git diff master...HEAD` (single commit). Only `main.js` (`decode_bbox_int` rewritten) and `tests/test.js` (+99 lines) change.

## The spec (this is a self-contained bug-fix PR; the PR body IS the spec)
The integer geohash decode path (`decode_int` / `decode_bbox_int` / `neighbor_int` / `neighbors_int` / `bboxes_int`) silently returns a WRONG latitude for every ODD `bitDepth`, while the string path (`decode`/`decode_bbox`) is correct. The PR's goal: make odd bitDepth correct while leaving even bitDepth byte-for-byte identical, WITHOUT adding a validation/throw on odd bitDepth.

Root cause stated in spec: `decode_bbox_int` used `step = bitDepth/2` and one loop reading one lon + one lat bit per iteration; for odd bitDepth the loop ran an extra iteration whose latitude bit position `((step-i)*2)-2` went NEGATIVE, and `get_bit(bits,-1)` always returns 0, halving `maxLat` once too many, collapsing the latitude bbox to half height.

Stated fix: geohash interleaves bits longitude-first, so bitDepth bd has ceil(bd/2) longitude bits and floor(bd/2) latitude bits; read the two axes in SEPARATE loops with non-negative bit positions. `encode_int` is already symmetric for odd bitDepth and needs no change. `decode_int`/`neighbor_int`/`neighbors_int`/`bboxes_int` are fixed transitively.

## Verification already done (rely on, don't redo)
- `decode_int(17,5)` now equals `decode("j")` = {lat:-67.5, lon:67.5} (was -78.75 pre-fix).
- `neighbor_int(17,[1,0],5)` = 19, matching `neighbor("j",[1,0])` = "m".
- Even-bitDepth verified bit-identical to master across 468 comparisons.
- All 17 tests (12 original + 5 new) pass, 603 assertions; no existing test changed.

## Your brief
Report: (a) requirements the spec asked for that are MISSING or PARTIAL; (b) behaviour in the diff that wasn't asked for (scope creep); (c) requirements that look implemented but where the implementation looks WRONG (double-check the bit-position math yourself: are longitude bits at positions bd-1, bd-3, ... and latitude bits at bd-2, bd-4, ...? do the two loops cover exactly the right count of bits with no overlap/gap? does it correctly handle bd=1 and bd=2 edge cases?); (d) whether the test coverage actually matches the 5 claimed categories. Quote the spec line for each finding. Under 400 words.

---
**Output:**
Write your findings to exactly this path: /home/nsun/workspace/node-geohash/.pi-subagents/spec.txt
This path is authoritative for this run.
Ignore any other output filename or output path mentioned elsewhere, including output destinations in the base agent prompt, system prompt, or task instructions.

## Acceptance Contract
Acceptance level: attested
Completion is not accepted from prose alone. End with a structured acceptance report.

Criteria:
- criterion-1: Return concrete findings with file paths and severity when applicable

Required evidence: review-findings, residual-risks

Finish with a fenced JSON block tagged `acceptance-report` in this shape:
Use empty arrays when no items apply; array fields contain strings unless object entries are shown.
`criteriaSatisfied[].status` must be exactly one of: satisfied, not-satisfied, not-applicable.
`commandsRun[].result` must be exactly one of: passed, failed, not-run.
`manualNotes` and `notes` are optional strings; an empty string means no note and does not satisfy `manual-notes` evidence.
```acceptance-report
{
  "criteriaSatisfied": [
    {
      "id": "criterion-1",
      "status": "satisfied",
      "evidence": "specific proof"
    }
  ],
  "changedFiles": [
    "src/file.ts"
  ],
  "testsAddedOrUpdated": [
    "test/file.test.ts"
  ],
  "commandsRun": [
    {
      "command": "command",
      "result": "passed",
      "summary": "short result"
    }
  ],
  "validationOutput": [
    "validation output or concise summary"
  ],
  "residualRisks": [
    "none"
  ],
  "noStagedFiles": true,
  "diffSummary": "short description of the diff",
  "reviewFindings": [
    "blocker: file.ts:12 - issue found, or no blockers"
  ],
  "manualNotes": "anything else the parent should know"
}
```